# Database Configuration
# For SQLite (default):
DATABASE_URL=sqlite:///quickquiz.db
# For PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/quickquiz
# For MySQL:
# DATABASE_URL=mysql://user:password@localhost:3306/quickquiz

# Flask Configuration
SECRET_KEY=your-flask-secret-key-change-this-in-production
JWT_SECRET=your-jwt-secret-key-change-this-in-production

# GroqCloud API Configuration (for AI question generation and evaluation)
# Get your API key from: https://console.groq.com/
GROQ_API_KEY=your-groq-api-key-here

# Optional: Flask Environment
# FLASK_ENV=development
# FLASK_DEBUG=1

# Logging Configuration
# LOG_LEVEL: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO
# LOG_FORMAT: json (structured) or text (human-readable) (default: json)
LOG_FORMAT=json
# LOG_FILE: Optional file path for log output (default: stdout only)
# LOG_FILE=logs/app.log
